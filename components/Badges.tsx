export default function Badges({ report }: { report: any }) {
  // Heuristic: show likely frameworks (stub)
  return (
    <div className="flex gap-2 mb-2">
      <span className="bg-blue-100 text-blue-800 px-2 py-1 rounded text-xs">Framework: Unknown (stub)</span>
    </div>
  );
}
